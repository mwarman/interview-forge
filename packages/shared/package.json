{
  "name": "@interview-forge/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "clean": "rimraf coverage dist",
    "test": "vitest run --silent passed-only",
    "test:coverage": "vitest run --coverage --silent passed-only"
  }
}
